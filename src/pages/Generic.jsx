import { BarChart3, Trophy, Settings, Sparkles } from 'lucide-react'
export default function Generic({ name }) { const data = { Contests: { icon: Trophy, title: 'Contest management', sub: 'Create, schedule and monitor coding contests.', cards: ['Upcoming contests', 'Registrations', 'Leaderboard integrity'] }, Analytics: { icon: BarChart3, title: 'Platform analytics', sub: 'Track growth, engagement, problem quality and judge health.', cards: ['Daily active users', 'Submission funnel', 'Problem acceptance'] }, Settings: { icon: Settings, title: 'Platform settings', sub: 'Configure roles, judge infrastructure, notifications and content policies.', cards: ['Admin roles', 'Judge workers', 'Notification rules'] } }[name] || { icon: Sparkles, title: name, sub: 'Manage your platform from a centralized workspace.', cards: ['Configuration', 'Overview', 'Activity'] }; const I = data.icon; return <div className="space-y-6"><div className="rounded-3xl bg-gradient-to-br from-slate-950 via-slate-900 to-indigo-950 p-7 text-white shadow-xl"><div className="flex max-w-2xl items-start gap-4"><div className="rounded-2xl bg-white/10 p-3"><I size={24} /></div><div><div className="text-xs font-semibold uppercase tracking-[0.16em] text-indigo-200">coding village</div><h2 className="mt-2 text-3xl font-bold tracking-tight">{data.title}</h2><p className="mt-2 text-slate-300">{data.sub}</p></div></div></div><div className="grid gap-5 md:grid-cols-3">{data.cards.map((c, i) => <div key={c} className="rounded-2xl border border-slate-200 bg-blue-100 p-6 shadow-sm"><div className="text-xs font-semibold uppercase tracking-wide text-slate-400">Module {i + 1}</div><h3 className="mt-3 font-bold">{c}</h3><p className="mt-2 text-sm leading-6 text-slate-500">A dedicated workspace is ready here for the next backend integration.</p><button className="mt-5 rounded-xl border border-slate-200 px-3 py-2 text-xs font-semibold hover:bg-slate-50">Configure</button></div>)}</div></div> }
